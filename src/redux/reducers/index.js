import {combineReducers} from "redux";
import layoutData from "./layoutData";
import liveLayoutData from "./liveLayoutData";

// export default combineReducers({ layoutData});
export default combineReducers({layoutData, liveLayoutData});
